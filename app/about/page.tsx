import { AboutClient } from '@/components/about/AboutClient'

export const metadata = {
  title:       'About — MASS',
  description: 'MASS is an AI automation and software agency. We build systems that make businesses run faster — delivered in weeks, not months.',
}

export default function AboutPage() {
  return <AboutClient />
}
